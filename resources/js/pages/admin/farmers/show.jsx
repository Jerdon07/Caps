import AdminLayout from "@/layouts/admin-layout";

export default function Show({ farmer }) {
    console.log(farmer)

    return (
        <AdminLayout>
            Show user
        </AdminLayout>
    )
}